 # Example .env file for FramePack
# Copy this file to .env and edit as needed for your environment

SERVER=0.0.0.0
PORT=7880
# Set to true to create a public, temporary link to your app via Gradio's servers.
# Not recommended for production or private deployments.
SHARE=false
# Set to true to open the app in your browser after launch (local desktop use only).
# Set to false for Docker or headless/server environments.
INBROWSER=false
MODEL_DIR=/path/to/your/models
HF_TOKEN=
# Add other options as needed